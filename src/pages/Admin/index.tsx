import { PageContainer } from '@ant-design/pro-components';

const AdminPage: React.FC = () => {
  return (
    <PageContainer ghost>
      <p className="pt-4 text-base">
        管理后台骨架已就绪，管理能力按 docs/architecture 的实施路线图逐步交付。
      </p>
    </PageContainer>
  );
};

export default AdminPage;
